# Оптимизация логирования в консоли

## Проблема
В консоли отображалось слишком много логов, включая повторяющиеся сообщения о найденных кошельках и других технических деталях, что затрудняло чтение важной информации.

## Решение
Реализована система логирования с уровнями важности и возможностью управления выводом логов.

## Новые возможности

### 1. Система уровней логирования
- **ERROR** (0) - Критические ошибки
- **WARN** (1) - Предупреждения  
- **INFO** (2) - Важная информация
- **DEBUG** (3) - Отладочная информация
- **VERBOSE** (4) - Подробная отладочная информация

### 2. Управление логированием кошельков
- Возможность включить/выключить логирование кошельков
- По умолчанию отключено для уменьшения спама в консоли

### 3. Управление логированием транзакций
- Возможность включить/выключить детальное логирование транзакций
- По умолчанию отключено

### 4. Визуальный интерфейс управления
В правом верхнем углу экрана появился компонент "Logging Controls" с возможностями:
- Выбор уровня логирования
- Включение/выключение логирования кошельков
- Включение/выключение логирования транзакций
- Сброс настроек к значениям по умолчанию

## Что изменилось

### Убраны избыточные логи:
- ❌ `Found embedded wallet: {address: '0x...', walletClientType: 'privy', ...}`
- ❌ `Loaded cached gas parameters from storage`
- ❌ `Loaded cached chain parameters from storage`
- ❌ `Loaded cached RPC health from storage`
- ❌ `Loaded cached nonce data from storage`
- ❌ `Using RPC endpoint for chain ${chainId}: ${healthyRpcUrl}`
- ❌ `Auto-reset circuit breaker for chain ${chainId} - trying again`

### Оставлены важные логи:
- ✅ `Embedded wallet ready: 0x...` (только при первом нахождении)
- ✅ `Creating embedded wallet...` (при создании)
- ✅ `Embedded wallet created successfully` (при успешном создании)
- ✅ `🎮 Game started successfully`
- ✅ `🎮 Game ended successfully`
- ⚠️ `No embedded wallet found - faucet may send to wrong wallet` (критические ошибки)

## Как использовать

### Через интерфейс:
1. Найдите компонент "Logging Controls" в правом верхнем углу
2. Выберите нужный уровень логирования
3. Включите/выключите логирование кошельков и транзакций по необходимости
4. Используйте кнопку "Reset to Defaults" для возврата к настройкам по умолчанию

### Программно:
```javascript
import { 
  setLogLevel, 
  setWalletLogging, 
  setTransactionLogging,
  LOG_LEVELS 
} from './config/logging';

// Установить уровень логирования
setLogLevel(LOG_LEVELS.INFO);

// Включить логирование кошельков
setWalletLogging(true);

// Включить логирование транзакций
setTransactionLogging(true);
```

## Настройки по умолчанию
- **Development режим**: INFO уровень
- **Production режим**: WARN уровень
- **Логирование кошельков**: отключено
- **Логирование транзакций**: отключено

## Преимущества
1. **Чистая консоль** - только важная информация
2. **Гибкость** - можно включить детальное логирование при необходимости
3. **Производительность** - меньше операций логирования
4. **Удобство отладки** - легко переключаться между уровнями детализации

## Файлы изменений
- `src/config/logging.js` - новая система логирования
- `src/components/LoggingControls.jsx` - компонент управления
- `src/hooks/useBlockchainUtils.js` - оптимизировано логирование
- `src/services/blockchainService.js` - оптимизировано логирование
- `src/config/blockchain-performance.js` - оптимизировано логирование
- `src/App.jsx` - добавлен компонент управления